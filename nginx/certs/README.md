# Mount your TLS certificates here:
#   fullchain.pem   ← certificate + intermediates
#   privkey.pem     ← private key
#
# Then uncomment the ssl_* lines in nginx/nginx.conf
# This directory is gitignored — never commit real certs.
